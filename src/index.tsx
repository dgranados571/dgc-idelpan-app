import ReactDOM from 'react-dom/client';
import IdelpanApp from './idelpanApp';
import './index.css';

const root = ReactDOM.createRoot(
  document.getElementById('root') as HTMLElement
);
root.render(  
    <IdelpanApp />
);

